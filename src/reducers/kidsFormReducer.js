const INITIAL_STATE = {
    id: 'child1',
    name: 'name',
    picture: '',
};

export const kidsFormReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
